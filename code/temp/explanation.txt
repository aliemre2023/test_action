Here's a concise summary of the changes:

1. **GitHub Workflow Update**:
   - Upgraded `actions/checkout` from v3 to v4
   - Added Python 3.10 setup step
   - Implemented diff capture to `diff.txt`
   - Added dependency installation and script execution steps
   - Included cleanup step for diff file

2. **Python Script Changes**:
   - Replaced CLI argument input with file-based diff reading
   - Added ASCII sanitization and length truncation (60k chars max)
   - Enhanced error handling with try/except blocks
   - Added debug logging and output validation
   - Now writes summary to `explanation.txt` file
   - Uses environment variable for API key with fallback

3. **Dependencies**:
   - Added `openai==1.55.0` to requirements.txt

The changes automate diff processing, improve error resilience, and transition to file-based I/O instead of CLI arguments.